# 在 MiniMind-V 上验证 Projector 泛化界预测结果的完整实验方案

## 0. 方案结论

本实验可以在 MiniMind-V 上完成，而且最合适的验证范围是：

> 固定并冻结 SigLIP2 视觉编码器与 MiniMind 语言模型，只在 caption alignment pretraining 阶段训练 Projector；分别使用 Projector Subspace 和 Projector-SubLoRA 两种低维参数化，对最终低维向量进行实际量化，并按 Lotfi 等人的口径计算可实现的 prefix-free 描述长度上界；在量化后模型上计算 prediction-smoothed、image-cluster-level BPD 泛化证书。里程碑 4 和 5 只验证固定候选中哪些配置得到非空证书；里程碑 6 在两种压缩方法内分别选择 bound 最低的代表模型，再比较两个代表模型的 bound 与 held-out validation BPD 排序。

本实验验证的不是“泛化界能精确等于 validation 误差”，而是以下两个问题：

1. **非空性**：证书是否严格优于均匀预测器的 BPD，即是否小于 $\log_2 V$；
2. **方法代表比较**：在不使用 validation 选择代表的前提下，两种压缩方法各自 bound 最低的代表，其 bound 方向是否与 raw held-out validation BPD 方向一致。

本方案的数学证书只讨论同分布 caption alignment pretraining 风险，不把结论扩展到原始无界 NLL、VQA accuracy、幻觉率、组合泛化或分布外泛化。held-out validation clusters 与训练簇来自同一份 MiniMind-V caption pretraining 数据和同一个固定 image-cluster 划分机制，因此比外部生成 caption 更贴近证书的目标总体；validation 只在两个方法代表模型按 bound 选定后用于描述其相对表现，不把经验排序等同于数学证明。

---

## 1. 实验目标与分析假设

### 1.1 主要研究问题

设第 $j$ 个实验配置产生量化重建模型 $\widehat h_j$，其最终证书为 $B_j$，held-out validation 图像簇上的 raw BPD 为 $\operatorname{BPD}^{\mathrm{raw}}_{j,\mathrm{val}}$。主要研究问题是：

- 固定候选中哪些配置满足
  $$
  B_j<\log_2 V;
  $$
- 两种压缩方法各自按 $B_j^{\mathrm{final}}$ 选出的 best-bound representative，其 bound 方向是否与两个代表模型的 raw validation BPD 方向一致。

### 1.2 分析假设

- **H1（非空性）**：固定候选中至少有一个配置得到 $B_j<\log_2V$；
- **H2（方法代表比较）**：在两种方法都存在 non-vacuous representative 的前提下，Subspace 与 Projector-SubLoRA 各自的 best-bound representative，其 bound 较小的一方具有较低的 raw validation BPD；该比较是描述性结果，不把 validation 用于代表选择。

候选模型族以及训练、量化和证书计算规则已在各自执行前冻结。里程碑 6 的两个方法代表必须只依据冻结的泛化界分别选择，validation 只能在代表选定后用于一次性评估，不能用于重新训练、调整量化器、选择 $\alpha$ 或事后删减候选集合。

候选配置共享训练数据、认证抽样和 validation set，不能被当作独立重复实验。若要做严格确认性复现，必须在训练任何候选模型前重新冻结一个未观察的同源划分，并在打开 validation 前冻结候选族、选择规则和报告内容。

---

## 2. 模型、冻结底座与条件信息

### 2.1 MiniMind-V 实验模型

第一版实验统一使用一个预先固定、与当前训练集独立的 canonical caption prompt

$$
Q_0=\text{``Describe this image.''}.
$$

模型实际条件分布写成

$$
h_{u_P}(I,Q_0,Y_{<t})
=
G_{\theta_0}\!\left(P_{\omega(u_P)}(E_{\psi_0}(I)),Q_0,Y_{<t}\right),
$$

后文为简洁可省略固定条件 $Q_0$，但所有实际前向、训练和认证评估都必须使用同一个 $Q_0$、conversation template、tokenizer 和 label mask。prompt token 是条件上下文，不作为 caption 标签计入 BPD。

其中：

- $E_{\psi_0}$：固定的 SigLIP2 视觉编码器；
- $G_{\theta_0}$：固定的 MiniMind LLM；
- $P_{\omega(u_P)}$：唯一允许由当前图文训练集学习的 Projector；
- $u_P$：真正训练、量化和编码的低维参数。

MiniMind-V 当前 Projector 为

$$
\operatorname{LayerNorm}(768)
\rightarrow
\operatorname{Linear}(768,768)
\rightarrow
\operatorname{GELU}
\rightarrow
\operatorname{Linear}(768,768).
$$

包含 LayerNorm 参数和 Linear bias 时，Projector 总参数量为

$$
D_P
=2\times768+2\times(768^2+768)
=1,182,720.
$$

### 2.2 可以免费条件化的 side information $A$

以下对象必须在读取当前图像簇训练集之前固定，并记录版本或哈希：

- SigLIP2 checkpoint、配置和 image processor；
- MiniMind LLM checkpoint、tokenizer 和词表；
- Projector 架构；
- Projector 基准初始化 $\omega_0$ 的生成算法与固定 seed；
- 随机子空间算法、参数展平顺序和固定 seed；
- 固定 canonical prompt $Q_0$、conversation template、图像预处理规则与 label mask 规则；
- 量化规则、算术编码长度上界的计费约定和模型参数化映射；
- 软件版本中会影响量化模型评估确定性的部分。

建议生成 `frozen_manifest.json`，至少记录：

```text
vision_checkpoint_sha256
llm_checkpoint_sha256
tokenizer_sha256
projector_init_seed
subspace_seed
parameter_name_and_shape_list
projection_algorithm_version
image_processor_config
canonical_prompt
conversation_template_sha256
label_mask_rule_version
git_commit
```

如果 $\omega_0$ 是在当前训练图像上得到的完整 Projector 权重，就不能免费放入 $A$。第一版实验应使用固定随机初始化的 Projector；或者使用在确定独立的外部数据上预训练且在当前样本产生前已经固定的 Projector。

### 2.3 冻结检查

训练开始前执行自动检查：

```python
for p in model.parameters():
    p.requires_grad = False

# 完成 Subspace 或 SubLoRA 包装后
trainable = {
    name: p.numel()
    for name, p in model.named_parameters()
    if p.requires_grad
}
print(trainable)
```

最终应该只有一个长度为 $d_P$ 的 `subspace_params`。如果还有 LLM、SigLIP2、完整 Projector weight、bias 或 LayerNorm 参数可训练，则当前实验不再属于“只编码 $u_P$”的理论设置。

---

## 3. 数据协议：以独立图像簇而不是行或 token 为样本

### 3.1 数据来源

使用 MiniMind-V 的 caption-only pretraining 数据，例如 `pretrain_i2t.parquet`。只保留用于图像描述/图文对齐的 caption 样本，不把多轮 SFT、VQA 或其他任务混入本实验。

实验报告必须说明数据的来源混合规则。理论中的总体分布是该数据生成/抽样机制对应的图像簇分布。如果原始公开语料是人工拼接、过滤或存在来源相关性，则“IID 图像簇”是实验假设，应在论文限制中明确说明，不能把结论无条件推广到任意互联网图像。

### 3.2 图像去重和分簇

当前主划分采用 `exact-image-cluster split`：

1. 对解码并执行 EXIF transpose、RGB 转换后的图像，将宽、高和规范化像素组成固定 payload 并计算 SHA-256；
2. 具有相同规范化像素 SHA-256 的全部记录及其 captions 放入同一个 image cluster；
3. 同一 exact-image cluster 绝不能跨越 train/validation 边界；
4. 当前主划分没有执行感知哈希或其他 near-duplicate filtering，因此不声称重编码、缩放或视觉近似图像一定不会跨 split。

在第一版固定 prompt 设置下，记最终独立图像簇为

$$
Z_i=\{(I_i,Y_{i1}),\ldots,(I_i,Y_{im_i})\},
\qquad i=1,\ldots,N_{\mathrm{all}}.
$$

理论样本量是图像簇数，不是 parquet 行数、caption 数、有效 token 数，也不是 $64N$ 个视觉 token。

### 3.3 Train/Held-out Validation 划分

在任何模型训练、超参数选择和结果查看前，对唯一图像簇做一次固定随机排列：

- 前 90% 作为训练簇 $S_N$，数量记为 $N=N_{\mathrm{eff}}$；
- 后 10% 作为 held-out validation 簇 $S_{\mathrm{val}}$，数量记为 $N_{\mathrm{val}}$。

held-out validation set 不参与任何模型的梯度训练，也不参与泛化证书的经验风险、复杂度项、认证子采样或 $\alpha$ 选择。理想协议要求它在所有候选模型、量化器、证书和分析规则固定后才打开。

若还需要调学习率或排查训练失败，只能从训练簇内部划出 development split。该 development split 仍属于被训练流程观察的数据，不能充当 held-out validation set。

#### 3.3.1 当前固定划分与实际时间顺序

当前数据构建结果固定为：MiniMind-V `pretrain_i2t.parquet` 共 1,274,698 行，经规范化像素 SHA-256 精确聚类后得到 623,733 个 image clusters；使用 NumPy PCG64、seed `20260814`，在按 cluster SHA-256 排序后一次性随机排列，其中 561,359 个簇进入训练 manifest，62,374 个簇进入 held-out validation manifest。当前构建没有执行近重复检测，因此严谨表述应是“按规范化像素精确聚类后固定留出”，不能声称已经排除了所有感知近重复。

采用固定表述：

> 我们在数据构建阶段从 MiniMind-V caption pretraining 数据中固定留出 10% 的 image clusters 作为 held-out validation set。该集合不参与两种压缩方法候选模型的梯度训练或泛化证书计算。里程碑 6 先在两种压缩方法内分别按训练侧泛化界选择一个代表，再报告两个代表的 held-out validation BPD 并比较其方向。

这里“不参与泛化证书计算”具体表示：62,374 个 validation clusters 不进入训练总体 manifest，不进入 10,000 次有放回认证抽样，不参与量化、模型描述长度、$\alpha$ 搜索或任何证书项。选定模型的 evaluation 必须使用同一量化重建模型、同一固定 prompt、tokenizer、图像预处理、label mask 和 image-cluster 等权风险定义。

但是，时间顺序限制必须与上述数据隔离事实同时报告：该 held-out 集合曾用于最初 S-4K 的一次性测试，因此里程碑 6 的 Bound–Validation 结果属于论文式、描述性的事后评估，不能支持新的确认性盲测声明。

### 3.4 Caption 和图像簇损失

对 caption $Y=(y_1,\ldots,y_T)$，只统计 caption 答案位置；固定 prompt $Q_0$、64 个 image placeholders、padding 以及 label 为 `-100` 的位置不计入风险，但 $Q_0$ 仍作为模型条件。定义

$$
\ell_\alpha(h;I,Q_0,Y)
=-
\frac{1}{T}
\sum_{t=1}^{T}
\log_2\left[(1-\alpha)p_h(y_t\mid I,Q_0,y_{<t})+\frac{\alpha}{V}\right].
$$

对同一图像簇内的多个 captions 先等权平均：

$$
\ell_\alpha(h;Z_i)
=
\frac1{m_i}\sum_{k=1}^{m_i}\ell_\alpha(h;I_i,Q_0,Y_{ik}).
$$

然后再对图像簇等权平均。不能把所有 token 混合后求一个总体均值，否则长 caption 和 caption 数较多的图像会获得更大权重，风险对象将与定理不一致。

held-out validation 必须直接使用固定留出 manifest 中与训练数据相同来源、相同 schema 的原始 caption，不做重新生成、翻译、内容筛选或人工改写。validation 风险与认证风险采用完全相同的“caption 内 token 等权、cluster 内 caption 等权、cluster 间等权”定义；区别仅在于它们对应不同且不重叠的 image clusters。

如果后续实验保留原始数据中的不同 prompts，则不能在理论中隐去 prompt。此时应定义

$$
Z_i=\{(I_i,Q_{i1},Y_{i1}),\ldots,(I_i,Q_{im_i},Y_{im_i})\},
$$

并把簇损失写成 $\ell_\alpha(h;I,Q,Y)$ 的平均；同一图像的所有 prompt-caption 对仍必须留在同一 cluster。第一版为减少额外随机性，不采用这一扩展。

### 3.5 冻结视觉特征缓存

SigLIP2 完全冻结，因此可以缓存 $E_{\psi_0}(I)$ 以加速训练。每幅图像的 FP16 特征约占

$$
64\times768\times2\ \text{bytes}\approx96\ \text{KiB}.
$$

总磁盘量约为 $96N_{\mathrm{all}}$ KiB，应采用分 shard 存储、内存映射或按需缓存。缓存必须由固定 checkpoint 和固定 processor 在 `eval()` 模式生成，并保存 image ID 到特征的确定性映射。

---

## 4. 两种核心 Projector 参数化

### 4.1 方法 S：Projector Subspace

按照固定的参数名顺序，将 Projector 的 LayerNorm、两个 Linear 的权重和 bias 展平为 $\omega\in\mathbb R^{D_P}$，令

$$
\boxed{\omega(u_P)=\omega_0+P_Pu_P},
\qquad
u_P\in\mathbb R^{d_P},\quad d_P\ll D_P.
$$

其中 $P_P\in\mathbb R^{D_P\times d_P}$ 不显式存成稠密矩阵，而参考 SubLoRA 官方实现，使用由固定 seed 生成的 RDKronQR/Lazy structured projector。训练时唯一的 `nn.Parameter` 是初始化为零的 $u_P$。

该方法与 `VLM泛化误差界推广_new.tex` 中的

$$
\omega=\omega_0+P_Pu_P
$$

完全一致，不需要修改主定理。

### 4.2 方法 SL：Projector-SubLoRA

只把两个 Linear 层替换为 LoRA 形式；LayerNorm 和两个 Linear bias 在第一版中固定：

$$
W_\ell(u_P)
=W_{\ell,0}
+\frac{\lambda_{\mathrm{LoRA}}}{r}
B_\ell(u_P)A_\ell(u_P),
\qquad \ell\in\{1,2\}.
$$

连接全部 LoRA 因子：

$$
\phi_P
=
[\operatorname{vec}(A_1),\operatorname{vec}(B_1),
  \operatorname{vec}(A_2),\operatorname{vec}(B_2)].
$$

如果两个 Linear 都是 $768\times768$，则 LoRA 参数环境维数为

$$
D_{\mathrm{LoRA}}=2r(768+768)=3072r.
$$

再使用固定随机子空间：

$$
\boxed{\phi_P(u_P)=\phi_{P,0}+P_Pu_P},
\qquad u_P\in\mathbb R^{d_P}.
$$

标准初始化可以取 $A_{\ell,0}$ 为固定 seed 生成的随机矩阵、$B_{\ell,0}=0$，从而 $u_P=0$ 时 LoRA 更新为零。因为 $B_\ell A_\ell$ 对低维坐标是非线性的，这才是严格意义上的 Projector-SubLoRA。

在文稿中描述该方法时，应写成

$$
\phi_P=\phi_{P,0}+P_Pu_P,
\qquad
\omega=\omega_0+\operatorname{LoRA}_P(\phi_P),
$$

而不能继续把它记成线性的 $\omega=\omega_0+P_Pu_P$。压缩定理本身不要求参数化映射对 $u_P$ 线性，因此统计证明不变；只需保证给定 $A$、结构选择和量化后的 $\widehat u_P$ 能唯一确定 $\widehat h$。

为避免与 prediction smoothing 参数混淆，LoRA scaling 不记作 $\alpha$，统一记为 $\lambda_{\mathrm{LoRA}}$。

---

## 5. M1–M5 历史固定实验矩阵

里程碑 1–5 使用以下 7 个历史固定配置。它们与里程碑 6 的新增 9 候选属于两个不同的实验矩阵，不应合并为同一候选集合：

| ID     | 方法     | LoRA rank$r$ | intrinsic dimension$d_P$ | 量化级别$Q_P$ |
| ------ | -------- | -------------: | -------------------------: | --------------: |
| S-1K   | Subspace |             — |                       1024 |              11 |
| S-4K   | Subspace |             — |                       4096 |              11 |
| S-16K  | Subspace |             — |                      16384 |              11 |
| SL4-1K | SubLoRA  |              4 |                       1024 |              11 |
| SL4-4K | SubLoRA  |              4 |                       4096 |              11 |
| SL8-1K | SubLoRA  |              8 |                       1024 |              11 |
| SL8-4K | SubLoRA  |              8 |                       4096 |              11 |

建议固定：

- Projector 初始化 seed：一个预先公布的整数；
- subspace seed：137，可与官方 SubLoRA 对齐；
- $\lambda_{\mathrm{LoRA}}=32$ 或预先固定的其他值；
- LoRA dropout：第一版建议设为 0，以简化重建与训练/评估差异；
- 固定训练 epoch 数和最终 epoch checkpoint；
- 固定优化器、学习率调度和 batch 构造规则。

这些训练设置需要预先冻结，以保证实验公平、可复现并避免使用 validation 调参；这并不意味着它们都要进入模型码长。如需多个训练随机种子做稳定性实验，应在实验前列出种子集合；若使用认证子样本同时比较其输出模型，则按第 10.2 节对多个固定模型做同时控制。

### 5.1 模型描述所需结构信息与唯一计费口径

模型描述是否需要包含某项信息，采用以下标准：

> 给定 side information $A$、量化后的 $\widehat u_P$ 和相应结构信息，是否能够唯一确定最终量化假设 $\widehat h$？

若对方法、intrinsic dimension、rank 和量化级别使用预先给定的有限 ID code，则其结构字段长度可以分解记为

$$
C_{\mathrm{hp}}
=
\left\lceil\log_2|\mathcal M|\right\rceil
+\left\lceil\log_2|\mathcal D|\right\rceil
+\left\lceil\log_2|\mathcal R|\right\rceil
+\left\lceil\log_2|\mathcal Q|\right\rceil
+C_{\mathrm{other}},
$$

其中 $\mathcal M$ 表示 Subspace/SubLoRA 方法选择。只对 SubLoRA 计入 rank，使用条件码可以避免给 Subspace 浪费 rank bits。$C_{\mathrm{hp}}$ 是模型原始描述长度 $C_{\mathrm{VLM}}$ 内部的一个分项，不能在形成 $K_{\mathrm{VLM}}$ 后再次添加。

通常需要编码或预先固定在 $A$ 中的对象包括：method、$d_P$、SubLoRA rank、$Q_P$、LoRA scaling、LoRA 插入位置、参数展平顺序，以及决定 $P_P$ 或 $\omega_0$ 的 seed。learning rate、optimizer、warmup、batch size、epoch 数、gradient accumulation 和只影响训练路径的随机 seed 通常不需要编码，因为最终假设已经由量化参数及其固定参数化映射确定。checkpoint epoch 只要不改变未编码的基准权重，也不需要编码。

即使训练阶段比较过多个 learning rate 或 optimizer，只要最终假设已经纳入预先规定的可编码假设族，第一层压缩界就对该假设族同时控制，不需要再为不影响最终模型表示的训练算法搜索单独增加 $\log_2$ 候选数。如果某个选择改变了未在 $A$ 中预先固定的基准模型、结构或投影，则该选择本身必须进入模型描述。

全文遵循 Lotfi 等人的论文口径，统一采用：

$$
\boxed{
K_{\mathrm{VLM}}
=\text{预先规定编码方案下的 prefix-free 模型描述长度上界}.}
$$

因此最终联合描述长度上界是

$$
K_{\mathrm{joint}}=K_{\mathrm{VLM}}+L_\alpha(\alpha),
$$

不得再写成 $K_{\mathrm{VLM}}+C_{\mathrm{hp}}+L_\alpha(\alpha)$。$C_{\mathrm{hp}}$ 只用于组成原始描述长度并上界化 $K_{\mathrm{VLM}}$。本实验不要求实际生成模型二进制文件；只要求编码方案预先规定、可唯一解释，并且各项长度上界显式可计算。

---

## 6. 训练流程

### 阶段 A：准备和锁定数据

1. 生成所有图像的规范化 ID；
2. 完成规范化像素 SHA-256 的 exact-image clustering；
3. 固定 train/held-out-validation cluster 划分；
4. 生成并保存 `split_manifest.json`；
5. 锁定 validation 簇，训练程序不得加载 validation 索引；
6. 可选地缓存冻结的 SigLIP2 特征。

### 阶段 B：构建固定底座

1. 加载 SigLIP2 和 MiniMind LLM；
2. 加载固定 tokenizer；
3. 用固定 seed 初始化 Projector；
4. 保存冻结对象哈希；
5. 将 SigLIP2、LLM 和固定 Projector 初值设为不可训练；
6. 构建 Subspace 或 SubLoRA wrapper；
7. 断言只有 `subspace_params` 可训练。

### 阶段 C：Projector-only alignment pretraining

训练仍可使用普通 caption cross-entropy/NLL：

$$
\widehat L_{mathrm{train}}
=-
\frac1{\text{valid tokens}}
\sum\log p_h(y_t\mid I,Q_0,y_{<t}).
$$

prediction smoothing 主要用于最后的有界风险评估，不强制要求训练目标也平滑。训练过程中：

- 视觉编码器始终处于 `eval()` 且无梯度；
- LLM 参数无梯度；
- 对最终 checkpoint 的选择规则必须预先固定；
- 每个样本使用固定 canonical prompt $Q_0$ 和固定 conversation template；
- 日志同时记录 token-level 训练 loss 和 image-cluster-level 诊断 loss，但二者不能混用；
- 不生成认证子样本索引；
- 不读取 held-out validation set。

训练算法可以包含随机 batch 顺序、训练 dropout 或预先规定的数据增强。这些随机性可以影响最终学到的 $\widehat u_P$，但只要它们不改变模型描述所需的固定参数化规则，就不进入模型码长。它们也不能出现在最终 certificate evaluator 中。

### 阶段 D：量化或量化感知微调

训练完成后量化 $u_P$。可以参考 SubLoRA 的做法，对量化中心和低维坐标进行少量 straight-through quantization-aware fine-tuning，但它仍然只能使用完整训练簇，且必须发生在认证子样本抽取之前。

完成量化感知微调后：

1. 固定最终 symbol assignments 和 FP16 centers；
2. 由 centers 和 assignments 显式构造 $\widehat u_P$；
3. 按固定参数化映射构造量化模型 $\widehat h$；
4. 计算 symbol entropy、codebook、counts 和结构选择的描述长度上界；
5. 计算并保存量化模型 checkpoint 哈希；
6. 从此不得再更新模型，所有进入证书的风险只在该量化模型上计算。

---

## 7. 量化与论文式模型描述长度上界

### 7.1 低维向量量化

对 $u_P\in\mathbb R^{d_P}$ 使用 $Q_P$ 个 FP16 centers：

$$
s_j=\arg\min_{q\in[Q_P]}|u_{P,j}-c_q|,
\qquad
\widehat u_{P,j}=c_{s_j}.
$$

定义

$$
n_q=\sum_{j=1}^{d_P}\mathbf 1\{s_j=q\},
\qquad
\widehat p_q=\frac{n_q}{d_P}.
$$

### 7.2 算术编码长度上界

遵循 Lotfi 等人的处理方式，根据量化 bin 的经验概率，symbol sequence 的算术编码存在如下长度上界：

$$
C_{\mathrm{symbol},P}
\le
\left\lceil d_PH_2(\widehat{\boldsymbol p})\right\rceil+1.
$$

本实验把它作为预先规定算术编码方案的可实现长度上界，不要求程序真正输出 arithmetic-coded bits。为了使该长度能够唯一解释模型，仍需显式计入量化 centers 和经验频率/count table：

$$
C_{\mathrm{codebook},P}=16Q_P,
\qquad
C_{\mathrm{count},P}
=Q_P\left\lceil\log_2(d_P+1)\right\rceil.
$$

因此 Projector 参数块的原始描述长度上界为

$$
\boxed{
\overline C_P
=
\left\lceil d_PH_2(\widehat{\boldsymbol p})\right\rceil
+1+16Q_P
+Q_P\left\lceil\log_2(d_P+1)\right\rceil.}
$$

这里报告的是由明确编码方案保证的描述长度上界，而不是磁盘 checkpoint 大小，也不是实际落盘 bitstream 长度。只要编码约定在观察当前训练集之前固定，并已将 codebook、counts 和结构选择的成本全部计入，就可以用于压缩泛化界。

### 7.3 总模型描述长度与 prefix-free 上界

仅训练 Projector 时，定义普通模型描述长度上界

$$
C_{\mathrm{VLM}}
=C_{\mathrm{hp}}+\overline C_P.
$$

当前实现对正整数原始描述长度 $C_{\mathrm{VLM}}$ 使用 Elias-gamma 长度前缀。长度头本身占

$$
2\left\lfloor\log_2 C_{\mathrm{VLM}}\right\rfloor+1
$$

bits，因此代码和冻结 manifest 中实际采用的、完全可计算的 prefix-free 描述长度上界为

$$
\boxed{
K_{\mathrm{VLM}}
=C_{\mathrm{VLM}}
+2\left\lfloor\log_2 C_{\mathrm{VLM}}\right\rfloor+1.}
$$

这里不再保留未指定的常数项。实现中的 `C_VLM_raw_upper_bits` 对应 $C_{\mathrm{VLM}}$，`prefix_overhead_upper_bits` 对应上式第二项，`K_VLM_upper_bits` 对应两者之和。该式与冻结 registry 中的 `elias_gamma_of_positive_raw_length` 编码约定一致；证书采用的是可实现长度上界，不要求实验中真的把 bitstream 写入磁盘。

最终联合描述长度上界为

$$
\boxed{
K_{\mathrm{joint}}
=K_{\mathrm{VLM}}+L_\alpha(\alpha).}
$$

$C_{\mathrm{hp}}$ 已经进入 $C_{\mathrm{VLM}}$，所以不能在 $K_{\mathrm{VLM}}$ 外重复添加。$\alpha$ 不包含在 $C_{\mathrm{hp}}$ 中，而是通过 $L_\alpha(\alpha)$ 单独支付。

实验应报告以下分项：

- `symbol_entropy_code_upper_bits`；
- `codebook_bits`；
- `count_bits`；
- `structure_choice_bits`；
- `C_VLM_raw_upper_bits`；
- `prefix_overhead_upper_bits`；
- `K_VLM_upper_bits`；
- `L_alpha_bits`；
- `K_joint_upper_bits`。

最终证书只代入一次 `K_VLM_upper_bits`，不得在其外再次加 `C_hp`。不要使用磁盘 checkpoint 的字节数，因为 PyTorch serialization header、未冻结参数副本等不等于理论模型描述。

### 7.4 量化与描述长度上界检查

每个模型必须通过以下测试：

```text
u_hat 的每个坐标都等于相应 FP16 center
counts 之和等于 d_P
经验概率之和等于 1
symbol entropy 由最终 assignments 重新计算
codebook、counts 和结构选择成本均进入 C_VLM
C_hp 没有在 K_VLM 外重复添加
所有 certificate loss 仅由量化后的 h_hat 计算
```

本方案不再要求真实 bitstream、独立 decoder、round-trip、endianness 或编码端/解码端 logits 一致性测试。证书所约束的对象仍然必须是由最终 FP16 centers 和 assignments 构造出的量化模型 $\widehat h$，不能使用量化前模型代替。

---

## 8. 有限精度 prediction smoothing 参数

允许

$$
\alpha=\frac{m}{2^b},
\qquad b\in\mathbb N,\qquad
1\le m\le2^b-1,\qquad m\text{ 为奇数}.
$$

其自定界码长为

$$
\boxed{L_\alpha(\alpha)=b+2\lfloor\log_2b\rfloor}.
$$

实际程序可为了有限计算量搜索一个有限子集，例如所有 $b\le16$ 的约分 dyadic 值，或预先规定的可计算子集。最终只支付被选中 $\alpha$ 的 $L_\alpha(\alpha)$，不再额外支付有限网格大小。

模型只需前向一次并保存每个正确 token 的原始概率 $p_h(y_t\mid I,Q_0,y_{<t})$，随后可向量化计算大量候选 $\alpha$：

$$
-\log_2\left[(1-\alpha)p+\frac\alpha V\right].
$$

计算时使用 FP64 或稳定的 log-domain 形式，避免小概率下溢。

### 8.1 Certificate evaluator 的确定性要求

最终 certificate evaluator 必须是从图像簇与最终量化模型到损失的确定性映射。认证和测试阶段统一要求：

- 使用 `model.eval()`，关闭所有 dropout；
- 使用固定 canonical prompt $Q_0$；
- 固定 tokenizer、conversation template、label mask 和截断规则；
- 固定图像 resize、normalize 等预处理，不使用随机 crop 或随机图像增强；
- 不使用 system-prompt augmentation、随机 prompt 模板、随机遮挡、随机 token dropout 或随机 caption 选择；
- 固定同一图像内 captions 的顺序，并对全部预定 captions 按第 3.4 节规则平均；
- 只评估由最终 FP16 centers 和 assignments 构造的量化模型。

训练阶段的随机数据增强、dropout、batch 顺序和优化噪声属于训练算法，可以存在，也通常不进入模型码长；它们不能延续到认证评估阶段。最终被定理控制的对象必须是确定的 $\ell_\alpha(\widehat h;Z_i)$。

损失区间为

$$
a_\alpha=-\log_2\left(1-\alpha+\frac\alpha V\right),
\qquad
b_\alpha=\log_2\frac V\alpha,
$$

区间宽度为

$$
\boxed{
\Delta_\alpha
=\log_2\left(1+\frac{(1-\alpha)V}{\alpha}\right)}.
$$

---

## 9. 认证子采样的时间顺序

这一部分必须严格遵守“先定模型、后抽认证子样本”。正确顺序是：

```text
训练全部预先固定的模型
→ 完成量化/QAT
→ 计算并锁定模型描述长度上界
→ 固定每个量化模型及其哈希
→ 再生成认证抽样随机种子
→ 从 N 个训练图像簇中有放回抽取 n 个索引
→ 计算认证子样本损失
→ 最后选择 alpha
```

对每个固定模型，从训练图像簇中有放回均匀抽取

$$
J_1,\ldots,J_n
\overset{\mathrm{i.i.d.}}{\sim}
\operatorname{Uniform}\{1,\ldots,N\}.
$$

建议第一版取

$$
n=10{,}000,
\qquad
\delta_1=0.025,
\qquad
\delta_2=0.025,
$$

从而总证书失败概率为 $\delta=0.05$。抽样必须有放回，因为当前定理第二层按 IID 有放回抽样证明。若改成无放回，需要改用有限总体集中不等式并重新写第二层。

认证子样本不能用于更新模型、量化中心、symbol assignments、$d_P$、LoRA rank 或 checkpoint。当前定理只允许在观察认证损失后选择 $\alpha$。

---

## 10. 单模型证书与多模型同时比较

### 10.1 单个预先确定模型

对量化重建模型 $\widehat h$，定义

$$
\widehat R^{\mathrm{sub}}_{\alpha,n}(\widehat h)
=\frac1n\sum_{s=1}^n
\ell_\alpha(\widehat h;Z_{J_s}).
$$

证书为

$$
\boxed{
\begin{aligned}
B(\widehat h,\alpha)
=\;&\widehat R^{\mathrm{sub}}_{\alpha,n}(\widehat h)\\
&+\Delta_\alpha
\sqrt{
\frac{[K_{\mathrm{VLM}}+L_\alpha(\alpha)]\ln2+\ln(1/\delta_1)}{2N}}
\\
&+\Delta_\alpha
\sqrt{
\frac{L_\alpha(\alpha)\ln2+\ln(1/\delta_2)}{2n}}.
\end{aligned}}
$$

在可计算搜索集合中选择

$$
\widehat\alpha
\in\arg\min_\alpha B(\widehat h,\alpha).
$$

### 10.2 同时比较 $M$ 个模型

当前文稿第二层只对 $\alpha$ 做了同时控制，前提是模型在抽取认证索引前已经确定。若要使用同一批认证索引同时报告或比较 $M$ 个预先固定模型，应在所有模型固定后才抽取索引，并使用 Bonferroni 分配

$$
\delta_{2,j}=\frac{\delta_2}{M}.
$$

于是第 $j$ 个模型的第二层变为

$$
\Delta_\alpha
\sqrt{
\frac{L_\alpha(\alpha)\ln2+\ln(M/\delta_2)}{2n}}.
$$

这种修正允许以至少 $1-\delta_1-\delta_2$ 的联合概率同时报告全部 $M$ 个模型。即使所有模型复用同一组 $J_s$，Bonferroni 仍成立，不要求模型间或证书间独立。

另一种严格做法是：在抽认证样本前，仅根据训练集信息预先选定一个最终模型，然后只对它使用单模型公式。不能先看各模型的认证界、再选最小者，却仍使用没有 $\ln M$ 修正的第二层。

第一层压缩界已经通过模型 prefix code 对所有可解码模型同时控制，因此多模型修正主要发生在只对固定模型证明的第二层认证子采样事件。

---

## 11. 非空性与结果判定

### 11.1 两种平凡基准

损失值域给出的逐点平凡上界为

$$
b_\alpha=\log_2\frac V\alpha.
$$

均匀预测器的 BPD 为

$$
R_{\mathrm{unif}}=\log_2V.
$$

论文中的主要 non-vacuous 标准采用更严格、也更有意义的

$$
\boxed{B_j<\log_2V}.
$$

若 MiniMind tokenizer 的实际词表为 $V=6400$，则

$$
\log_2V\approx12.644\ \text{bits/token};
$$

程序必须从实际模型配置读取 $V$，不能硬编码 6400。

### 11.2 必须报告的证书分解

对每个模型报告：

$$
B_j
=
\underbrace{\widehat R^{\mathrm{sub}}_j}_{\text{量化经验风险}}
+
\underbrace{\varepsilon_{\mathrm{model},j}}_{K_{\mathrm{VLM}}+L_\alpha}
+
\underbrace{\varepsilon_{\mathrm{sub},j}}_{n\text{ 的子采样误差}}.
$$

这样可以区分证书较差究竟来自：

- Projector 欠拟合；
- 量化造成风险上升；
- 低维向量熵或描述长度上界过大；
- $N$ 太小；
- 认证子样本 $n$ 太小；
- 选中的 $\alpha$ 太小导致 $\Delta_\alpha$ 过大。

---

## 12. Held-out Validation 上的证书预测能力

### 12.1 数据用途与时间顺序

held-out validation manifest 固定包含 62,374 个 image clusters，占全部 623,733 个精确图像簇的约 10%。它不参与：

- 两种压缩方法候选模型的梯度训练或 checkpoint 选择；
- 量化中心、assignments 或模型描述长度计算；
- 10,000 次有放回认证子抽样及其经验风险；
- $\widehat\alpha_j$ 或 $B_j^{\mathrm{final}}$ 的计算。

这与 Lotfi 等人的论文式分析一致：bound 从训练数据及其认证子样本得到，再报告按 bound 选择的模型在 validation data 上的 BPD。当前分析不得根据 validation 结果重新训练、改变量化器、改变 $\alpha$、删除配置或修改选择规则。

时间顺序上，旧 validation set 在初始 S-4K 闭环后已经打开。因此里程碑 6 结果必须标记为论文式、描述性的事后 Bound–Validation 评估。

### 12.2 Validation 风险定义

“常规 validation BPD”明确指不做 prediction smoothing 的 held-out caption BPD。对第 $i$ 个 validation image cluster 中的 $m_i$ 条 caption，令第 $k$ 条 caption 的有效标签长度为 $T_{ik}$，则量化重建模型的常规指标定义为

$$
\boxed{
\operatorname{BPD}^{\mathrm{raw}}_{j,\mathrm{val}}
=
-\frac{1}{N_{\mathrm{val}}}
\sum_{i=1}^{N_{\mathrm{val}}}
\frac{1}{m_i}
\sum_{k=1}^{m_i}
\frac{1}{T_{ik}}
\sum_{t=1}^{T_{ik}}
\log_2 p_{\widehat h_j}
\!\left(y_{ikt}\mid I_i,Q_0,y_{ik,<t}\right).}
$$

它先在每条 caption 的有效标签 token 内平均，再在同一图像簇的 captions 间平均，最后在 image clusters 间平均。该指标用于论文式 Bound–Validation 对比；它是未平滑的有限样本经验指标，不作为本方案的有界损失证书，也不用于选择或重算 $B_j^{\mathrm{final}}$。

如需补充 prediction-smoothed validation risk，只对里程碑 6 的两个代表模型计算，不用于代表选择：

$$
\widehat R_j^{\mathrm{val}}(\alpha)
=
\frac1{N_{\mathrm{val}}}
\sum_{i=1}^{N_{\mathrm{val}}}
\ell_{\alpha}(\widehat h_j;Z_i^{\mathrm{val}}),
\qquad N_{\mathrm{val}}=62{,}374.
$$

不得使用量化前 checkpoint。validation 与认证评估必须共享固定 prompt `Describe this image.`、64 个 image placeholders、tokenizer、max sequence length、label mask、图像预处理和 cluster weighting。

每个模型的最终优化证书仍定义为

$$
\widehat\alpha_j
\in
\arg\min_{\alpha\in\mathcal A_{\mathrm{pre}}}B_j(\alpha),
\qquad
\boxed{B_j^{\mathrm{final}}=B_j(\widehat\alpha_j)}.
$$

报告两种互补口径：

| 目的                          |                          Certificate |                                                      Validation metric | 解释                                                                       |
| ----------------------------- | -----------------------------------: | ---------------------------------------------------------------------: | -------------------------------------------------------------------------- |
| 论文式 Bound–Validation 对比 | 两个方法代表的$B^{\mathrm{final}}$ | 两个方法代表各自的$\operatorname{BPD}^{\mathrm{raw}}_{\mathrm{val}}$ | 先在每种方法内按训练侧泛化界选择代表，再报告两个代表的未平滑 held-out 性能 |

最终报告只使用各模型预先冻结的 $B_j^{\mathrm{final}}$；不得在看到 validation 后重算证书或改变选择规则。

### 12.3 Bound–Validation 对比

里程碑 4 和 5 不进行 validation 评估；它们只检查每个冻结候选的证书是否非空，即 $B_j<\log_2V$。

里程碑 6 在所有候选的证书冻结后，按方法分层选择两个代表：

$$
j^*_{\mathrm{S}}=\operatorname*{arg\,min}_{j\in\mathcal M_{\mathrm{S}}}B_j^{\mathrm{final}},
\qquad
j^*_{\mathrm{SL}}=\operatorname*{arg\,min}_{j\in\mathcal M_{\mathrm{SL}}}B_j^{\mathrm{final}}.
$$

其中 $\mathcal M_{\mathrm{S}}$ 和 $\mathcal M_{\mathrm{SL}}$ 分别是 Subspace 与 Projector-SubLoRA 的冻结候选集合；若同一方法内出现并列，按预先冻结的 candidate ID 顺序取第一项。随后在 held-out validation clusters 上一次性计算两个代表模型的 raw validation BPD：

$$
\left(B_{j^*_{\mathrm{S}}}^{\mathrm{final}},\operatorname{BPD}^{\mathrm{raw}}_{j^*_{\mathrm{S}},\mathrm{val}}\right)
\quad\text{and}\quad
\left(B_{j^*_{\mathrm{SL}}}^{\mathrm{final}},\operatorname{BPD}^{\mathrm{raw}}_{j^*_{\mathrm{SL}},\mathrm{val}}\right).
$$

这一步参考 Lotfi 等人的论文式操作：每种方法的代表模型由训练数据上的证书选择，validation BPD 只在两个代表选定后作为独立的性能描述，不参与候选筛选、重训练、量化、$\alpha$ 选择或成功标准修改。Bound–Validation 对比只作描述性结果，不声称 validation 误差被数学证书直接控制。

---

## 13. 结果报告

### 13.1 主结果表

| 方法 | $r$ | $d_P$ | $Q_P$ | $\widehat\alpha$ | 量化前训练 BPD | 量化后认证 BPD | $K_{\mathrm{VLM}}$ 上界 (bits) | 模型项 | 子采样项 | 最终优化证书$B^{\mathrm{final}}$ | non-vacuous |
| ---- | ----: | ------: | ------: | -----------------: | -------------: | -------------: | -------------------------------: | -----: | -------: | ---------------------------------: | ----------: |

主结果表列出全部冻结候选及其证书非空性。另设一张 Bound–Validation 对比表，列出 Subspace 与 Projector-SubLoRA 各自按最小泛化界选出的代表、最终泛化界和未平滑 held-out validation BPD。除这两张表外，不制作其他图形或曲线。

| 方法 | 代表 configuration ID | 代表泛化界 | 代表 raw validation BPD | non-vacuous |
| ---- | --------------------- | ---------: | ----------------------: | ----------- |

---

## 14. 关键伪代码

### 14.1 Projector Subspace

```python
class IntrinsicProjector(nn.Module):
    def __init__(self, base_projector, intrinsic_dim, seed):
        super().__init__()
        self.names, self.shapes, self.init_params = freeze_and_flatten(base_projector)
        self.D = sum(p.numel() for p in self.init_params)
        self.u = nn.Parameter(torch.zeros(intrinsic_dim))
        self.P = RoundedDoubleKronQR(self.D, intrinsic_dim, seed=seed)

    def forward(self, x):
        delta = self.P @ self.u
        params = unflatten_add(self.init_params, delta, self.shapes)
        return functional_call_projector(params, x)
```

优先使用 `torch.func.functional_call` 或明确的 functional Linear/LayerNorm，而不是在每次 forward 中删除和重新赋值 `nn.Parameter`，以提高与 DDP、checkpoint 和新版 PyTorch 的兼容性。

### 14.2 证书计算

```python
for model in frozen_quantized_models:
    assert model.hash_is_final
    probs_by_cluster = evaluate_correct_token_probs(model, certificate_indices)

    best = None
    for alpha in encodable_alpha_search_set:
        empirical = cluster_smoothed_bpd(probs_by_cluster, alpha, vocab_size)
        L_alpha = alpha_code_length(alpha)
        delta_alpha = log2(1 + (1 - alpha) * vocab_size / alpha)

        eps_model = delta_alpha * sqrt(
            ((model.K_upper_bits + L_alpha) * log(2) + log(1 / delta1)) / (2 * N)
        )
        eps_sub = delta_alpha * sqrt(
            (L_alpha * log(2) + log(M / delta2)) / (2 * n)
        )
        bound = empirical + eps_model + eps_sub

        if best is None or bound < best.bound:
            best = Result(alpha, empirical, eps_model, eps_sub, bound)
```

如果最终只认证一个在抽样前已经确定的模型，把 `log(M / delta2)` 改回 `log(1 / delta2)`。

---

## 15. 必须通过的正确性检查

### 15.1 数据检查

- train/validation 没有相同 image hash；
- 完全相同的规范化像素 SHA-256 不跨 split；当前没有 near-duplicate filtering，不得作对应保证；
- 同一图像所有 captions 位于同一 cluster；
- 报告 $N$ 时使用 cluster 数；
- validation manifest 没有被任何训练程序或证书 evaluator 加载；
- validation 固定为 62,374 个精确图像簇，来源、split seed、manifest SHA-256 和历史打开时间均如实记录；
- validation 直接使用原始 MiniMind-V caption schema，不进行生成、翻译、筛选或人工改写；
- 报告明确区分“未参与训练/证书计算”与历史上曾被观察，并将里程碑 6 结果标为描述性的事后评估。

### 15.2 模型检查

- 只有 $u_P$ 具有梯度；
- SigLIP2 和 LLM 始终冻结；
- Projector 初值与子空间可以由 manifest 确定性恢复；
- Subspace 的参数顺序固定；
- SubLoRA 只作用于预先固定的两个 Linear；
- LayerNorm/bias 若被训练，已纳入 $u_P$ 或单独编码；
- 评估使用 `eval()`，所有 dropout 关闭；
- 固定 canonical prompt $Q_0$、conversation template、tokenizer 和 label mask；
- 认证和 validation 评估不存在随机文本或图像增强。

### 15.3 风险检查

- 使用 `log2`，单位是 bits/token；
- 只统计 caption 标签位置；
- prompt token 作为条件上下文，但不作为预测标签计入 BPD；
- 先 caption 内平均，再 cluster 内平均，再 cluster 间平均；
- smoothing 使用实际词表大小；
- 风险在量化重建模型上重新计算；
- 对 $p=0$、$p=1$ 和均匀预测器做解析单元测试；
- 每个 cluster loss 都落在 $[a_\alpha,b_\alpha]$ 内。

### 15.4 概率与时间顺序检查

- 模型和量化器先固定，认证索引后生成；
- 认证索引为有放回均匀抽样；
- 只有 $\alpha$ 可以在认证损失后选择；
- 同时比较多个模型时使用 $\delta_2/M$ 或等价的先验权重分配；
- validation 不参与 $\widehat\alpha$、证书或候选集合的选择；
- 论文式主表只并列报告两个方法代表的最终优化证书和各自未平滑 $\operatorname{BPD}^{\mathrm{raw}}_{\mathrm{val}}$；
- 共享 validation data 的候选不得当作独立重复实验，且历史打开状态排除了新的确认性盲测声明。

### 15.5 描述长度上界检查

- 使用最终 assignments 计算 $H_2(\widehat{\boldsymbol p})$；
- symbol 算术编码长度按 $\lceil d_PH_2(\widehat{\boldsymbol p})\rceil+1$ 上界；
- FP16 codebook 成本 $16Q_P$ 已计入；
- count table 成本 $Q_P\lceil\log_2(d_P+1)\rceil$ 已计入；
- method、$d_P$、$r$、$Q_P$ 等结构选择已计入 $C_{\mathrm{hp}}$ 或预先固定在 $A$ 中；
- $C_{\mathrm{hp}}$ 只进入 $C_{\mathrm{VLM}}$ 一次，不在 $K_{\mathrm{VLM}}$ 外重复计费；
- Elias-gamma 长度头 $2\lfloor\log_2 C_{\mathrm{VLM}}\rfloor+1$ 已计入，且 $K_{\mathrm{VLM}}=C_{\mathrm{VLM}}+2\lfloor\log_2 C_{\mathrm{VLM}}\rfloor+1$；
- learning rate、optimizer、epoch 等只影响训练且不决定最终模型表示的信息不强制编码；
- $\alpha$ 只通过 $L_\alpha(\alpha)$ 单独计费；
- 所有进入证书的风险只由最终量化模型计算。

---

## 16. 分阶段实施计划

### 里程碑 1：最小闭环

只训练一个 S-4K 模型：

1. 完成图像分簇和 train/held-out-validation 锁定；
2. 实现 Projector Subspace；
3. 训练并量化；
4. 按论文口径计算完整的模型描述长度上界；
5. 抽取 $n=10{,}000$ 个认证图像簇；
6. 搜索有限精度 $\alpha$；
7. 计算单模型证书；
8. 最后在 held-out validation 上报告 S-4K 性能。

只有该闭环通过全部单元测试后，再扩大实验矩阵。

### 里程碑 2：Subspace 候选验证

运行 S-1K、S-4K、S-16K，检验不同 $d_P$ 候选的证书非空性。

### 里程碑 3：Projector-SubLoRA

运行 SL4/SL8 配置，确保 theorem-side 参数化明确写成 $\phi_P=\phi_{P,0}+P_Pu_P$，并与 Subspace 做相似描述长度上界和相似风险的比较。

### 里程碑 4：候选模型统一认证与非空性验证

固定里程碑 2 和里程碑 3 产生的候选量化重建模型。在全部模型均已冻结后，从共同训练 image-cluster 总体中有放回均匀抽取共享的 10,000 次认证样本，并在第二层同时控制中使用候选总数 $M$ 的多模型修正。对每个模型计算并冻结：

- 最终优化证书 $B_j^{\mathrm{final}}=B_j(\widehat\alpha_j)$；
- 对应的最优可编码平滑参数 $\widehat\alpha_j$；
- 非空性指示 $\mathbf 1\{B_j^{\mathrm{final}}<\log_2V\}$。

该里程碑只验证证书是否非空，不进行 validation 评估，也不根据 validation 结果重新认证、改变 $\alpha$ 网格或修改模型集合。

### 里程碑 5：Held-out Validation 前的非空性复核

研究问题固定为：**全部候选的冻结泛化证书中是否存在非空界？**

使用数据构建阶段从 MiniMind-V caption pretraining 数据中固定留出的 62,374 个 image clusters（约 10%）作为 held-out validation set，但在本里程碑不读取该集合。它不进入候选模型的梯度训练、量化、描述长度、10,000 次认证抽样或泛化证书计算。

只报告每个候选的最终优化证书及其是否满足 $B_j^{\mathrm{final}}<\log_2V$，用于确认泛化界非空。

里程碑 5 不读取 validation。只有在里程碑 6 完成基于泛化界的两个方法代表选择后，才执行两个代表的 Bound–Validation 对比。

### 里程碑 6：两种方法代表的 Bound–Validation 对比

里程碑 6 在里程碑 4/5 的证书均已冻结后，按照 Lotfi 等人的论文式流程，在 Subspace 和 Projector-SubLoRA 候选中各选择一个泛化界最低的代表模型，再对两个代表进行一次 held-out validation 评估。

**文档版本说明**：本节是在里程碑 5 完成并观察其结果后新增的 v6 方案修订，不属于旧 `v5_heldout_validation_protocol_freeze_manifest.json` 所冻结的历史协议。不得为了纳入本节而重写、更新或重新签发任何 v5 冻结清单。当前九候选协议使用独立的 v6 修订冻结清单，绑定本节、9 候选 registry、QAT 实现、描述长度实现、认证公式、测试和执行入口的 SHA-256。

本里程碑由 6A 和 6B 两个严格分离的阶段组成。6A 不得访问任何 validation 模型输出；6B 只能在两个方法代表选择完成后读取 validation，并如实标注其历史时间顺序。

#### 里程碑 6A：两方法统一认证与代表选择

6A 只使用固定 training data 和在全部候选冻结后抽取的一份共享 certification sample，计算并比较

$$
\left(K_{\mathrm{VLM},j},\widehat R^{\mathrm{sub}}_j(\widehat\alpha_j),B_j^{\mathrm{final}}\right).
$$

里程碑 6 固定以下 6 个 Projector-SubLoRA 量化假设：

$$
r\in\{1,4\},\qquad
d_P\in\{256,512,1024\},\qquad
Q_P=11.
$$

另设三个纯 Projector Subspace 对照：

$$
d_P\in\{256,512,1024\},\qquad Q_P=11.
$$

因此候选族总数固定为 $M=9$。该九候选范围在部分正式训练已经开始后完成修订，结果中必须如实披露这一时间顺序，不能声称九候选协议在全部正式训练开始前冻结。修订后的 9 个候选不得根据 validation 结果或训练过程中的临时 validation loss 继续删减、增补或重命名。

统一训练与量化协议为：

1. 所有候选训练 4 epochs，并预先固定相同的数据顺序生成规则、优化器、学习率调度、全局 batch size、随机种子和最终 checkpoint 规则；正式模型只取第 4 epoch 结束时的状态，不做 validation-based early stopping；
2. 前 90% 训练步优化连续低维坐标，最后 10% 训练步执行量化感知训练（QAT）；QAT 的 straight-through estimator、量化中心初始化、中心更新规则、空 bin 处理、FP16 codebook 和最终最近中心指派必须在正式训练前实现、测试并冻结；
3. 所有候选固定使用 $Q_P=11$，不得在训练或认证阶段改变量化等级；
4. SigLIP2、MiniMind LLM、Projector 输入输出结构、prompt、tokenizer、图像预处理、label mask、caption weighting 和 image-cluster weighting 与前述正式协议保持一致；
5. 9 个模型的低维坐标、量化中心、符号计数、完整描述长度和量化重建参数全部冻结并独立复核后，才从 training clusters 中用新鲜 OS CSPRNG 种子有放回均匀抽取一份共享的 $n=10{,}000$ certification sample；不得复用里程碑 4 的认证索引；
6. 认证第二层同时控制使用 $M=9$；各模型的最终优化证书只使用预先冻结的可编码 $\alpha$ 网格，并支付相应编码成本；
7. 模型描述中的结构选择只计一次：使用预先冻结的 4-bit candidate ID 唯一确定参数化方法、$r$、$d_P$ 和 $Q_P$，即 $C_{\mathrm{hp},j}=4$ bits；不得在 candidate ID 之外再次加入 $\log|\mathcal M|+\log|\mathcal R|+\log|\mathcal D|+\log|\mathcal Q|$ 或逐字段结构成本。低维量化符号、FP16 codebook、count table 和总消息的 prefix-free 自定界开销仍须照常编码；
8. 里程碑 5 的正式产物保持只读。旧结果仅作为历史描述性参照，不能与使用不同认证样本和不同 $M$ 修正的新证书进行未经校准的数值优劣宣称。

正式执行前将下面的 candidate-ID lookup table 原样写入 v6 registry 和冻结清单：

| 4-bit ID                                                             | 参数化             | $r$ | $d_P$ | $Q_P$ |
| -------------------------------------------------------------------- | ------------------ | ----: | ------: | ------: |
| `0000`                                                             | Projector Subspace |    — |     256 |      11 |
| `0001`                                                             | Projector Subspace |    — |     512 |      11 |
| `0010`                                                             | Projector Subspace |    — |    1024 |      11 |
| `0011`                                                             | Projector-SubLoRA  |     1 |     256 |      11 |
| `0101`                                                             | Projector-SubLoRA  |     1 |     512 |      11 |
| `0111`                                                             | Projector-SubLoRA  |     1 |    1024 |      11 |
| `1001`                                                             | Projector-SubLoRA  |     4 |     256 |      11 |
| `1011`                                                             | Projector-SubLoRA  |     4 |     512 |      11 |
| `1101`                                                             | Projector-SubLoRA  |     4 |    1024 |      11 |
| `0100`, `0110`, `1000`, `1010`, `1100`, `1110`, `1111` | 未使用，禁止分配   |    — |      — |      — |

LoRA 插入位置、LoRA scaling、Projector 结构、RDKronQR 投影算法及阶数、共同初始化/训练/QAT/evaluation 算法等若对 9 个候选完全相同，并由修订后的 v6 冻结清单固定，则属于共同 side information $A$，不在每个模型中重复编码。若任何候选需要表外的结构差异，就必须在训练前扩展 lookup table 和 prefix code；不得在结果产生后把候选特有信息追认为共同 side information。

这里的 4-bit candidate ID 与认证第二层修正是两项不同成本，二者必须同时保留：

$$
\underbrace{C_{\mathrm{hp},j}=4\ \mathrm{bits}}_{\text{模型描述中的候选身份}}
\qquad\text{和}\qquad
\underbrace{\ln\frac{9}{\delta_{\mathrm{sub,family}}}}_{\text{9 模型共享认证样本的同时控制}}.
$$

前者只进入一次 $C_{\mathrm{VLM},j}$，并随其一起计算 prefix-free 的 $K_{\mathrm{VLM},j}$；后者只进入认证子采样复杂度项。不得把 4 bits 当成第二层修正，也不得因为已经使用 $M=9$ 的第二层修正而从模型描述中删掉 candidate ID。

6A 的主要判定不使用 validation。首先报告全部候选的

$$
K_{\mathrm{VLM},j},\quad
\widehat R^{\mathrm{sub}}_j(\widehat\alpha_j),\quad
\varepsilon_{\mathrm{model},j},\quad
\varepsilon_{\mathrm{sub}},\quad
B_j^{\mathrm{final}}.
$$

选择规则为分别在两种方法内取最小证书：

$$
j^*_{\mathrm{S}}=\operatorname*{arg\,min}_{j\in\mathcal M_{\mathrm{S}}} B_j^{\mathrm{final}},
\qquad
j^*_{\mathrm{SL}}=\operatorname*{arg\,min}_{j\in\mathcal M_{\mathrm{SL}}} B_j^{\mathrm{final}}.
$$

若同一方法内出现并列，按预先冻结的 candidate ID 顺序取第一项。两个选择都只使用训练侧证书，不使用 validation BPD。

#### 里程碑 6B：Post-hoc Held-out Evaluation

只有在全部量化假设及其证书冻结并独立复核、且 $j^*_{\mathrm{S}}$ 与 $j^*_{\mathrm{SL}}$ 已按上述规则确定后，才进入 6B。6B 在完整的 62,374 个 validation image clusters 上一次性评估这两个代表；不得依据 validation 结果继续训练、重新量化、改变 $\alpha$、调整候选集合或回写 6A 的选择规则。

选定模型使用与前述里程碑相同的确定性 evaluation contract，并报告

$$
\left(B_{j^*_{\mathrm{S}}}^{\mathrm{final}},\operatorname{BPD}^{\mathrm{raw}}_{j^*_{\mathrm{S}},\mathrm{val}}\right)
\quad\text{and}\quad
\left(B_{j^*_{\mathrm{SL}}}^{\mathrm{final}},\operatorname{BPD}^{\mathrm{raw}}_{j^*_{\mathrm{SL}},\mathrm{val}}\right).
$$

6B 固定报告一张 Bound–Validation 对比表：方法、代表 configuration ID、代表 $B^{\mathrm{final}}$、代表 $\operatorname{BPD}^{\mathrm{raw}}_{\mathrm{val}}$ 和 non-vacuous 状态，并报告两个代表之间的 bound 方向与 raw validation BPD 方向是否一致。

6B 只能回答：“两种压缩方法各自按训练侧最小泛化界选择的代表，其 bound 方向是否与 raw validation BPD 方向一致？”该结果是描述性 Bound–Validation 对比，不把 validation 表现等同于数学证明，也不将其推广为候选全集的排序结论。

---

## 17. 最终可以得出的结论边界

若数学证书非空并已完成预定的 Bound–Validation 对比，可以严谨地表述为：

> 在固定且与当前图像簇训练样本独立的 SigLIP2 和 MiniMind LLM 条件下，只训练并压缩编码跨模态 Projector。对于量化重建后的 Subspace/Projector-SubLoRA 模型，以至少预设置信度获得了证明所定义同分布总体上 prediction-smoothed caption BPD 的非空上界。里程碑 6 分别在 Subspace 与 Projector-SubLoRA 候选中按训练侧泛化界选择代表，并在固定留出的 held-out validation clusters 上比较两个代表的 bound 与 raw validation BPD 方向。由于该集合曾在早期闭环中打开，该结果属于描述性的事后评估，而非新的确认性盲测。

不能据此表述为：

- 原始未平滑 NLL 已被控制；
- SigLIP2 或 MiniMind LLM 本身在当前数据上的泛化得到了重新证明；
- 证书适用于分布外图像、VQA、幻觉率或任意下游任务；
- validation 上的经验结果等价于数学证明，或一次事后评估验证了证书的长期标称覆盖率；
- 该 held-out validation 在整个项目期间始终未被观察，或当前结果属于新的确认性盲测；
- 不同 $\widehat\alpha_j$ 下的平滑 BPD 可以直接作为同一风险进行模型排序；
- 视觉 token 数可以当作独立样本数。

---

## 18. 主要参考实现

- MiniMind-V：[https://github.com/jingyaogong/minimind-v](https://github.com/jingyaogong/minimind-v)
- SubLoRA bounds 官方实现：[https://github.com/Sanaelotfi/SubLoRA-bounds-for-LLMs](https://github.com/Sanaelotfi/SubLoRA-bounds-for-LLMs)
- 本实验使用的理论推导：`VLM泛化误差界推广_new.md`

SubLoRA 仓库主要用于参考四部分：LoRA 参数筛选、`IDModule` 式低维坐标训练、RDKronQR 结构化随机投影、低维向量量化与认证子采样流程。模型复杂度遵循原论文的口径，使用 symbol entropy、FP16 codebook、count table、结构选择成本和自定界开销得到可实现的 prefix-free 描述长度上界；不额外要求真实 bitstream 或独立 decoder。MiniMind-V 实验仍需针对 Projector、image-cluster 风险和可编码 $\alpha$ 做上述适配。
